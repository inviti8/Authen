# Authen — Phase 1 (registration gate)
#
# Deliberately minimal. IPFS pinning, the on-chain trust registry and C2PA manifest
# signing (c2pa-python) are Phase 2 and must not land on the Phase 1 critical path.

# `httpx` is not optional in practice: HTTPFacilitatorClientSync imports it lazily in
# _get_client(), so without it server.initialize() fails at first /supported call.
x402-avm[flask,avm,extensions,httpx]==2.0.2

flask==3.1.3
gunicorn==23.0.0

# Ed25519 signing for attestations, and X.509 for the C2PA app-CA in Phase 2.
# The whole product rests on this one being correct.
cryptography>=42,<47

# C2PA Content Credentials. Official CAI bindings over c2pa-rs. Needs Python >=3.10;
# we already require 3.11+ for tomllib.
#
# PIN DELIBERATELY: the certificate profile c2pa-rs accepts is undocumented and was
# determined empirically (see authen/c2pa_sign.py). Every rejection - wrong EKU,
# missing SubjectKeyIdentifier - surfaces as the same message, "the certificate is
# invalid". tests/test_c2pa.py pins the working shape; if a bump breaks it, that is
# where it shows.
c2pa-python==0.37.6
