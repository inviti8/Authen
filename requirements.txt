# PintheonV2 — Phase 1 (registration gate)
#
# Deliberately minimal. IPFS, TinyDB, aiposematic and the Stellar/Soroban ports are
# Phase 2 and must not land on the Phase 1 critical path.

# `httpx` is not optional in practice: HTTPFacilitatorClientSync imports it lazily in
# _get_client(), so without it server.initialize() fails at first /supported call.
x402-avm[flask,avm,extensions,httpx]==2.0.2

flask==3.1.3
gunicorn==23.0.0

# Preview scrambling + page handling. aiposematic proper arrives in Phase 2; Pillow
# carries the placeholder pipeline until then.
pillow==11.3.0
